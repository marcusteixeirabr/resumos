---
interface Props {
  src: string;
  alt: string;
}
const { src, alt } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');
const imgSrc = src.startsWith('/') ? `${base}${src.slice(1)}` : src;
---
<img src={imgSrc} alt={alt} class="inline-diagram" />
